# NEXT PHASE PLAN — 道路構造令本文の完全解析（次フェーズ）

> 重要: 本計画は設計書であり、**自動開始しない**。着手は別途指示を受けた場合のみ。

## 0. 前提

- 本フェーズ（目次リバースエンジニアリング）の成果物を入力とする。
- 本文解析は「道路構造令の解説と運用（令和3年3月）PDF」の**本文ページ**を対象とする。
  - PDF物理ページと印刷ページ番号の対応をまず確定する（本文は2-upスキャンと推定）。
- 作業場所: `~/Projects/spacer-clone-liner-r1-planning/docs/liner/research/road-structure-ordinance/` 配下（このフェーズの成果物を拡張）。
- `~/Projects/spacer-clone` は**変更禁止**。PDF原本のrepo収録は禁止。

## 1. 読む章の順番（= BODY_RESEARCH_PRIORITY.md の Phase RO-1〜RO-10）

1. Phase RO-1: 道路分類・設計速度・設計区間・設計車両（Ⅲ-1）
2. Phase RO-2: 道路横断構成（Ⅲ-2）
3. Phase RO-3: 平面線形（Ⅲ-3-1〜3-4）
4. Phase RO-4: 片勾配・拡幅・緩和区間（Ⅲ-3-5〜3-8）
5. Phase RO-5: 視距（Ⅲ-3-9）
6. Phase RO-6: 縦断線形（Ⅲ-3-10〜3-14）
7. Phase RO-7: 交差点・立体交差・ランプ・IC/JCT（Ⅲ-4〜5）
8. Phase RO-8: 橋梁連携（Ⅲ-8-5・建築限界）
9. Phase RO-9: 例外規定・特例・照査（第10章・適用の際の注意・用語）
10. Phase RO-10: 参考（Ⅰ・Ⅱ・6・7・9章）

## 2. 章ごとの目的

| Phase | 目的 |
| --- | --- |
| RO-1 | 道路種級・設計速度・設計車両の決定ロジックを抽出 |
| RO-2 | 横断構成要素の幅員・寸法基準を抽出 |
| RO-3 | 平面線形（R・曲線長）の基準値・適用条件を抽出 |
| RO-4 | 片勾配・拡幅・すりつけ・緩和区間の算定手法を抽出 |
| RO-5 | 視距の算定式・基準値を抽出 |
| RO-6 | 縦断勾配・縦断曲線・横断勾配・合成勾配の基準を抽出 |
| RO-7 | 交差点・ランプ・IC/JCTの幾何基準を抽出 |
| RO-8 | 橋梁・高架・建築限界とLINER上部工の接続点を確定 |
| RO-9 | 特例・例外・適用注意事項を抽出（Rule Engineの例外層） |
| RO-10 | 用語定義・参考事項を整理 |

## 3. 抽出する数値・条件・例外（各章共通テンプレート）

- 表（種級×設計速度→最小R 等）をCSV化
- 算定式（拡幅量・視距・すりつけ長・縦断曲線長等）を式テキスト化
- 適用条件（「ただし」「を除く」「特例」「望ましい値」等の修飾語を必ず捕捉）
- 条文番号との対応（政令第X条 ↔ 解説ページ）
- 例外・特例・適用除外のリスト

## 4. Rule Engine 候補（本フェーズで登録済み23件）

`RULE_ENGINE_CANDIDATES.csv` の23候補（RULE-01〜RULE-23）。
本文確認後、possible_inputs/outputs/constraints を確定し、数式・表を実装する。
本文未確認のため全行 `requires_body_review = YES`。

## 5. 実案件との照合方法

1. 抽出した基準値と、実案件（西知多道路・東海JCT）の線形計算書・設計図の値を突合。
   - 例: 最小曲線半径(R=400/500/1900) と設計速度・片勾配の整合、A=450 と緩和区間の整合。
2. 不一致・特例が妥当か（条文の特例・適用注意）を確認。
3. 照合結果を `ROAD_PROJECT_MAPPING.md` の「検証可能性」欄へ反映。

## 6. LINERデータモデルとの対応

- 抽出した基準値を、`LinerProject` / `HorizontalAlignment` / `VerticalAlignment` /
  `CrossSectionTemplate` / `StationEquation` へマッピングする案を作成。
- 現行ドメインモデルに無い項目（片勾配・拡幅・視距・建築限界照査・交差点）は、
  後続の設計フェーズでスキーマ拡張案を別途作成する（本フェーズでは実装しない）。

## 7. 実装前に必要な確認

- [ ] 設計エンジンの対象スコープ（道路のみ / 道路+橋梁一体）の確定
- [ ] Rule Engine の実行モデル（入力→判定→出力→警告）の合意
- [ ] 既存LINERのスキーマ変更範囲の合意（後方互換）
- [ ] 実案件資料の利用許諾・格納場所の確認
- [ ] OCR精度の不足個所（4-7・8-4・3-1-7 等）の画像確認または再OCR

## 8. 成果物（次フェーズ想定）

- `BODY_<Phase>.md`（各フェーズの本文読解メモ）
- `VALUE_TABLES/*.csv`（基準値表）
- `FORMULAS.md`（算定式）
- `RULE_ENGINE_SPEC.md`（実装仕様案）
- `ARTICLE_MAPPING.csv`（条文番号 ↔ 解説ページ ↔ LINER項目）

## 9. PR分割案（GitHubへの発行が指示された場合）

| PR | 内容 |
| --- | --- |
| PR-1 | 本文読解基盤（PDFページ↔印刷ページ対応表・OCRパイプライン） |
| PR-2 | RO-1 道路分類・設計速度（Value Table + Spec） |
| PR-3 | RO-3〜RO-4 平面線形・片勾配・拡幅・緩和（コア） |
| PR-4 | RO-6 縦断線形 |
| PR-5 | RO-2 横断構成 |
| PR-6 | RO-5 視距・RO-7 交差点・立体交差 |
| PR-7 | RO-8 橋梁連携・RO-9 例外 |
| PR-8 | Rule Engine 実装（別途指示時のみ） |

> PRは指示があるまで作成しない。本計画はworktree内の設計文書に留める。
