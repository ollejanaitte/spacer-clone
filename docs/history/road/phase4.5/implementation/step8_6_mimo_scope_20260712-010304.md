Now let me read the remaining critical files:
Let me check the remaining backend test fixtures and bridge API tests:
Let me check the result schema and the generatedModelParity:
Now I have all the information needed. Let me compile the comprehensive scope document.

# Step 8.6 Static / Eigen Analysis Result Parity — 事前スコープ調査

---

## 1. AnalysisResult の正確な型

**事実** (`frontend/src/types.ts:212-264`, `schemas/result.schema.json:7-405`)

```typescript
type AnalysisResult = {
  projectId: string;
  schemaVersion: "1.0.0";
  analysisSummary: {
    analysisType: "linear_static" | "eigen" | "response_spectrum" | "responseSpectrum" | "influence_line" | "time_history" | "moving_load";
    status: "success" | "warning" | "failed";
    startedAt: string; finishedAt: string; durationMs: number;
    nodeCount: number; memberCount: number; loadCaseCount: number;
    totalDof: number; freeDof: number; constrainedDof: number;
    solver: "scipy_sparse" | "scipy_eigh" | "newmark_beta" | "influence_line_reuse";
  };
  displacements: Array<{loadCaseId; nodeId; ux; uy; uz; rx; ry; rz}>;
  reactions: Array<{loadCaseId; nodeId; fx; fy; fz; mx; my; mz; constrainedDofs: string[]}>;
  memberEndForces: Array<{loadCaseId; memberId; coordinateSystem: "local"; i: EndForce; j: EndForce}>;
  eigenResult?: EigenResult;
  responseSpectrumResult?: ResponseSpectrumResult;
  influenceResult?: InfluenceResult;
  movingLoadResult?: MovingLoadResult;
  timeHistoryResult?: TimeHistoryResult | null;
  warnings: StructuredMessage[];
  errors: StructuredMessage[];
};
```

**注意**: `ProjectModel.analysisResults` は **timeHistory のみ永続化**。static/eigen/resultSpectrum は transient（API 経由のみ）。`types.ts:188-191` 参照。

---

## 2. static result の構成

**事実** (`backend/engine/results.py:29-116`)

`build_success_result()` は以下を生成:
- `displacements`: 全ノード × 全荷重ケース。グローバル座標系 (`DOF_NAMES = (ux,uy,uz,rx,ry,rz)`)。
- `reactions`: support ノードのみ。`assembly.stiffness @ u_full - f_full` で計算。`constrainedDofs` リスト付き。
- `memberEndForces`: `coordinateSystem: "local"` 固定。I端= `forces[:6]`, J端= `forces[6:]`。local axis で `k_local @ u_local - equiv` で計算。

---

## 3. displacement の座標系

**事実** (`backend/engine/results.py:49-54`, `backend/engine/dof.py:7`)

- **グローバル座標系**。`DOF_NAMES = ("ux", "uy", "uz", "rx", "ry", "rz")` は global座標。
- `u_full[dofs]` で node の 6DOF を取得。dof_map は `6 * node_index + offset`。
- フロント表示では `SpacerAxisSwap` で Viewer 表示のみ座標変換 (`coordinateTransform.ts:86-98`)。backend結果自体は変換されない。

---

## 4. reaction の座標系と符号

**事実** (`backend/engine/results.py:47-74`)

- **グローバル座標系**。`reaction_full = assembly.stiffness @ u_full - f_full`。
- 符号: **部材が構造物に及ぼす力（節点反力）**。外力と釣り合う方向。例: `test_engine_verification_cases.py:51` で `fixed_node["fy"] == P`（上向き反力）、`test_result_schema.py:48` で `fy: 10.0`。
- `constrainedDofs`: `"ux" | "uy" | "uz" | "rx" | "ry" | "rz"` の文字列配列。

---

## 5. memberEndForces の座標系

**事実** (`backend/engine/results.py:75-88`, `backend/engine/element.py:127-132`)

- **ローカル座標系固定** (`coordinateSystem: "local"`)。
- `transform = transformation(rotation)` で 12x12 ブロック対角の global→local 変換行列。
- `u_local = transform @ u_full[state.dofs]` → `forces = k_local @ u_local - equiv_local`。
- `forces[:6]` が I 端（nodeI側）、`forces[6:]` が J 端（nodeJ側）。

---

## 6. I/J 端の配列・field 構成

**事実** (`backend/engine/results.py:85-87`, `schemas/result.schema.json:407-437`)

```
i: { fx, fy, fz, mx, my, mz }   ← forces[0:6]
j: { fx, fy, fz, mx, my, mz }   ← forces[6:12]
```

- fx/fy/fz: 軸力・せん断力
- mx/my/mz: ねじり・曲げモーメント
- EndForce 型: `types.ts:534-541`

---

## 7. backend の local axis 構築規則

**事実** (`backend/engine/element.py:10-37`)

```python
x_axis = (node_j - node_i) / length          # 部材軸
candidate = orientation_array(...)            # orientationVector or orientationNode or fallback
y_axis = candidate - dot(candidate, x_axis) * x_axis  # 直交化
y_axis /= norm(y_axis)
z_axis = cross(x_axis, y_axis)               # 右手系
z_axis /= norm(z_axis)
y_axis = cross(z_axis, x_axis)               # 再直交化
rotation = vstack([x_axis, y_axis, z_axis])  # 3x3 回転行列
```

**fallback** (`element.py:69-72`): `orientationVector` なし → global Z を候補。Z と平行なら global Y。

---

## 8. orientationVector の意味

**事実** (`backend/engine/element.py:40-72`, `frontend/src/types.ts:44`)

- member の **y 軸方向の参照ベクトル**（正規化される）。
- `element.py:46-53`: 正規化して直接使用。
- `element.py:54-68`: `orientationNode` 場合は `(orientationNode - nodeI)` を正規化。
- **作用**: local y 軸の向きを定義。z 軸は cross(x, y) で決まる。

---

## 9. I/J 反転時の local axis 変化

**推定** (`element.py:10-37` の数学的帰結)

I/J を反転 (nodeI ↔ nodeJ) した場合:
- `x_axis` が **±180° 反転** (符号反転)。
- `y_axis`: `orientationVector` が同一なら、直交化の結果 **同一方向を保つ**。
- `z_axis = cross(x, y)`: x が反転 → **z も反転**。
- **結果**: x 反転、y 同一、z 反転。

ローカル座標の FORCE 分量への影響:
- fx (軸力): 反転
- fy (せん断): 同一
- fz (せん断): 反転
- mx (ねじり): 反転
- my (曲げ): 反転 → ただし端の定義も反転
- mz (曲げ): 同一 → ただし端の定義も反転

**推定**: 等価な部材荷重の local 変換も同様に影響。`assembly.py:102-103` で `rotation @ [wx,wy,wz]` が変化。

---

## 10. resultViewModel.ts の I端符号反転の意味

**事実** (`frontend/src/results/resultViewModel.ts:195-196`)

```typescript
const rawI = row.i[componentMap[component]];
const i = rawI === 0 ? 0 : -rawI;  // I端のみ符号反転
const j = row.j[componentMap[component]];  // J端はそのまま
```

**意味**: backend は **element end forces**（節点に作用する力）を返すが、表示は **internal section forces**（断面力図）に変換。I端の section force は element force と逆符号（作用・反作用）。J端はそのまま。

- テスト (`resultViewModel.test.ts:6-33`): M1 の mz: i=0, j=10 → 表示で i=0, j=10 (M2: i=-10→i=10, j=0)。2要素の moment diagram が連続する。

---

## 11. parity core で採用すべき canonical convention

**推定**

static/eigen result parity で採用すべき正規化:
- **displacement**: グローバル座標系（backend出力そのまま）。node ID非依存の座標ベース matching。
- **reaction**: グローバル座標系。support node + constrainedDofs で一致確認。
- **memberEndForces**: I/J 端の定義を canonical 化。候補:
  1. **node coordinate canonical**: I 端 = x 座標が小さい方（同値なら y, z）
  2. **endpoint key canonical**: node matching 後の canonical pair に従う
- **eigen**: mode number で matching。shape の ± 符号は無視（MAC で判定）。

---

## 12. I/J 反転時の候補変換表

**推定**

| 分量 | I/J未反転 | I/J反転時変換 |
|------|----------|-------------|
| fx (N) | I: +N | I: -N (x反転) |
| fy (Qy) | I: +Qy | I: +Qy (y同一) |
| fz (Qz) | I: +Qz | I: -Qz (z反転) |
| mx (Mx) | I: +Mx | I: -Mx (x反転) |
| my (My) | I: +My | I: -My (x反転+端反転) |
| mz (Mz) | I: +Mz | I: +Mz (x反転+端反転で2重反転) |

- J 端も同様。**完全な変換表**はbackend実装と element.py の回転行列数学に基づき fixture で検証が必要。

---

## 13. fixture で実測すべき事項

1. **単一 cantilever の backend 結果**: `sample_models.py:71-111` の case_1 で、I/J 端の各 force 分量を取得し、local axis 方向を手計算と照合。
2. **2要素 beam の I/J 接続点**: `test_engine_verification_cases.py:78-97` の case_2 で、N2 の I端 force == -J端 force（作用反作用）。
3. **orientationVector 変更時の force 変化**: 同一構造で orientationVector を変えて結果がどう変わるか。
4. **global load の member force**: `test_engine_verification_cases.py:124-208` の global uniform load テストで、local force の成分を確認。
5. **eigen mode shape の ± 符号**: `test_eigen_analysis.py:52-58` で `tip["rz"]` の符号と `tip["uy"]` の比を確認。
6. **eigen effective mass**: `test_eigen_analysis.py:205-281` で全方向の mass ratio/total を確認。

---

## 14. missing / null / empty / zero / failed の違い

**事実**

| 状態 | backend | frontend |
|------|---------|----------|
| **missing** | key自体が存在しない（JSONにない） | `undefined` |
| **null** | `null` 値（例: `timeHistoryResult: null`） | `null` |
| **empty** | 空配列 `[]`（例: `displacements: []`） | 空配列 |
| **zero** | `0.0` 数値（例: `ux: 0.0`） | `0` |
| **failed** | `status: "failed"`, error配列にエントリあり | `errors.length > 0` |

- `backend/engine/errors.py:45-76`: `error_result()` は空配列を返す。
- `resultViewModel.ts:180`: `result.errors.length > 0` で null を返す。

---

## 15. NaN / Infinity の backend/frontend 挙動

**事実** (`backend/engine/results.py:21-26`)

```python
def clean(value: float) -> float:
    if not math.isfinite(value):
        raise AnalysisError("POSTPROCESS_ERROR", "Result contains NaN or Infinity.")
    if abs(value) < 1e-14:
        return 0.0
    return float(value)
```

- backend: NaN/Infinity は `AnalysisError` で即座に失敗。
- frontend: `types.ts` の全数値は `number` 型。JSON parsing 後は `NaN` になりうるが、`assert_no_non_finite_numbers` (`backend/tests/assertions.py:23-31`) で検証済み。
- `tolerance.ts:30-31`: `compareScalarWithTolerance` は non-finite で `{ equal: false, delta: NaN }` を返す。

---

## 16. load case result identity

**事実** (`backend/engine/results.py:44-54`)

- `displacements` / `reactions` / `memberEndForces` の各行に `loadCaseId` が付与。
- 1つの `AnalysisResult` に **全load case の結果が混在**。
- frontend は `resultViewModel.ts:184` で `filter((row) => !loadCaseId || row.loadCaseId === loadCaseId)` でフィルタ。

**Parity で重要なこと**: load case は name + type で semantic matching し、結果は loadCaseId 経由で紐付ける。

---

## 17. node/member ID非依存 matching 方法

**事実** (`frontend/src/bridgeDefinition/semanticParity/nodeMatching.ts:20-109`, `memberMatching.ts:35-116`)

**Node matching**:
- 座標ベース。`coordinateKey()` で tolerance丸めした座標文字列を key として使用。
- 候補を distance でソートし、1:1 matching を実現。
- 暧昧な場合は ambiguity として報告。

**Member matching**:
- node matching 結果を用いて endpoint pair を projection。
- `canonicalPair(mappedI, mappedJ)` で endpoint key を正規化。
- sectionId + materialId は normalized key に含まれるが、matching では endpoint が主要。

---

## 18. EigenResult の正確な型

**事実** (`frontend/src/types.ts:281-300`, `schemas/result.schema.json:234-261`)

```typescript
type EigenResult = {
  massCaseId: string;
  normalization: "mass";  // const
  totalMassByDirection?: DirectionalValue[];  // optional
  modes: EigenModeResult[];
};

type EigenModeResult = {
  modeNo: number;
  eigenvalue: number;
  circularFrequency: number;  // ω
  frequency: number;          // f = ω/(2π)
  period: number;             // T = 2π/ω
  modalMass: number;
  participationFactors: DirectionalValue[];
  effectiveMassRatios: DirectionalValue[];
  effectiveMasses?: DirectionalValue[];                    // optional (E-1b)
  cumulativeEffectiveMassRatios?: DirectionalValue[];     // optional (E-1b)
  shape: EigenModeShape[];  // { nodeId, ux, uy, uz, rx, ry, rz }
};
```

**Schema required**: `modeNo, eigenvalue, circularFrequency, frequency, period, modalMass, participationFactors, effectiveMassRatios, shape`。`effectiveMasses`, `cumulativeEffectiveMassRatios` は optional。

---

## 19. eigenvalue / frequency / period の関係

**事実** (`backend/engine/eigen.py:288-298`)

```python
omega = math.sqrt(eigenvalue)                                    # ω = √λ
frequency = omega / (2.0 * math.pi)                             # f = ω/(2π)
period = (2.0 * math.pi) / omega                                # T = 1/f
```

- eigenvalue λ = ω² (固有角频率の2乗)。
- 3つは互いに変換可能。Parity 比較では全て比較すべきか、1つに集約すべきか検討要。

---

## 20. mode shape の成分順

**事実** (`backend/engine/eigen.py:310-319`)

```python
"shape": [
    {"nodeId": node.id} | dict(zip(DOF_NAMES, [clean(value) for value in full[dof_map.node_dofs(node.id)]]))
    for node in model.nodes
]
```

- `DOF_NAMES = ("ux", "uy", "uz", "rx", "ry", "rz")`。
- **全ノードを列挙**。mass が 0 の DOF も含む（値は recovery matrix で復元）。
- **グローバル座標系**。

---

## 21. mode shape normalization

**事実** (`backend/engine/eigen.py:283-292`)

```python
modal_mass = vector.T @ mass_matrix @ vector
normalized = vector / math.sqrt(modal_mass)
modal_mass = normalized.T @ mass_matrix @ normalized  # = 1.0
```

- **mass normalization**: `φᵀ M φ = 1` になるよう正規化。
- `EigenResult.normalization: "mass"` で記録。

---

## 22. ±反転同値

**推定**

- mode shape は任意のスケール倍で同一モード。`φ` と `-φ` は物理的に同一。
- MAC (Modal Assurance Criterion) で判定: `MAC = (φ₁ᵀφ₂)² / ((φ₁ᵀφ₁)(φ₂ᵀφ₂))`。
- MAC = 1.0 なら完全一致（±含む）。
- **Parity では**: mode matching 時に ± 符号を無視する必要がある。

---

## 23. MAC 計算方法

**未確認**: 現コードに MAC 計算は未実装。

**推奨実装**:
```typescript
function computeMAC(shapeA: EigenModeShape[], shapeB: EigenModeShape[]): number {
  // node matching 後、対応する DOF ベクトル a, b を作成
  // MAC = (a·b)² / ((a·a)(b·b))
}
```

- `EigenModeShape` は `types.ts:271-279`。
- node matching は座標ベース（既存 `nodeMatching.ts` を流用）。

---

## 24. near-repeated eigenvalue の matching

**未確認**: 現状 backend に near-repeated 対応なし。

**課題**: eigenvalue が近いモードは順序が solver で逆で逆転しうる（`eigh` は昇順だが、数値誤差で近い場合不安定）。

**推奨**: mode number での strict matching を基本とし、tolerance 付き eigenvalue 比較で補完。ambiguity として報告。

---

## 25. participation factor

**事実** (`backend/engine/eigen.py:367-377`)

```python
gamma = vector.T @ mass_matrix @ r   # r = 方向単位ベクトル
```

- X: `r = [1,0,0,0,0,0,...]`（ux DOF のみ 1.0）
- Y: `r = [0,1,0,0,0,0,...]`（uy DOF のみ 1.0）
- Z: `r = [0,0,1,0,0,0,...]`（uz DOF のみ 1.0）
- `direction_vector()` (`eigen.py:395-396`): `dof % 6 == offset` で判定。

---

## 26. effective mass / ratio

**事実** (`backend/engine/eigen.py:380-396`, `335-349`)

```
effective_mass_ratio = γ² / (rᵀ M r)
effective_mass = effective_mass_ratio × total_mass
```

- `total_mass = rᵀ M r`（方向別 total mass）。
- `cumulativeEffectiveMassRatios`: モード順に累積 (`eigen.py:352-364`)。

---

## 27. rigid body mode の扱い

**事実** (`backend/engine/eigen.py:140-145`)

```python
if float(np.min(eigenvalues)) <= tolerance:
    raise AnalysisError("MODEL_UNSTABLE", "Zero or negative eigenvalues were detected.")
```

- rigid body mode (eigenvalue ≈ 0) は **エラー** として検出し、solver は失敗。
- `model.py:486-491` で `analysisType != "linear_static"` を拒否。eigen は `eigen.py:18` 経由で直接実行。

---

## 28. result tolerance 候補

**事実** (`frontend/src/bridgeDefinition/semanticParity/tolerance.ts:3-8`)

```typescript
DEFAULT_SEMANTIC_TOLERANCE = {
  coordinate: { absolute: 1e-6 },
  length: { absolute: 1e-4, relative: 1e-6, floor: 1 },
  scalar: { absolute: 1e-9, relative: 1e-6, floor: 1e-9 },
  angle: { absolute: 1e-6 },
};
```

**result parity での推奨候補**:
- **displacement**: `scalar` tolerance (absolute: 1e-9, relative: 1e-6) を使用。座標変換の影響を考慮。
- **reaction**: `scalar` tolerance。backend の `clean()` (`results.py:21-26`) が `1e-14` 以下を 0.0 に丸め。
- **memberEndForces**: `scalar` tolerance。local axis の回転誤差を考慮し relative: 1e-5 程度。
- **eigenvalue**: 相対 tolerance 1e-5。固有値は桁が大きい場合がある。
- **mode shape**: MAC > 0.95 を equivalence threshold 候補。
- backend assertions: `REL_TOL = 1e-5`, `ABS_TOL_ZERO = 1e-9` (`backend/tests/assertions.py:7-8`)。

---

## 29. fixture-only test と solver integration test の境界

**事実**

- **fixture-only**: `test_result_schema.py` (手書き JSON)、`test_eigen_analysis.py` の手書き cantilever。
- **solver integration**: `test_engine_verification_cases.py` (`engine_runner` fixture で実行)、`test_engine_result_schema.py`。
- **Parity の境界**:
  - fixture-only: result の型・構造・座標系 convention を検証。
  - integration: backend → frontend の pipeline 全体を検証。

---

## 30. Step 8.6 / PR3 の非スコープ

**推定**

非スコープ:
- **responseSpectrum result parity** (既存の `responseSpectrumResult` は types.ts にあるが、parity core は未拡張)
- **influenceResult parity**
- **movingLoadResult parity**
- **timeHistoryResult parity** (永続化対象だが result parity とは別)
- **backend solver の変更**（Step 8.6 は result parity のみ）
- **frontend Viewer の変更**（表示は既存のまま）
- **非線形 / shear deformation** (`model.py:498-506` で明示的に拒否)

---

## 31. 変更候補ファイル

| ファイル | 変更内容 |
|---------|---------|
| `frontend/src/bridgeDefinition/semanticParity/types.ts` | `ResultParityCategory`, `ResultParityReport` 型追加 |
| `frontend/src/bridgeDefinition/semanticParity/index.ts` | result parity 関数 export |
| `frontend/src/types.ts` | 変更不要（既存型で十分） |
| `frontend/src/results/resultViewModel.ts` | 変更不要（既存の I端反転ロジックは正しい） |

---

## 32. 新規候補ファイル

| ファイル | 用途 |
|---------|------|
| `frontend/src/bridgeDefinition/semanticParity/resultParity.ts` | static result 比較コア |
| `frontend/src/bridgeDefinition/semanticParity/resultParity.test.ts` | static result parity テスト |
| `frontend/src/bridgeDefinition/semanticParity/eigenParity.ts` | eigen result 比較コア |
| `frontend/src/bridgeDefinition/semanticParity/eigenParity.test.ts` | eigen result parity テスト |
| `frontend/src/bridgeDefinition/semanticParity/resultNormalize.ts` | result 正規化（I/J canonical, ±反転除去） |
| `frontend/src/bridgeDefinition/semanticParity/mac.ts` | MAC 計算ユーティリティ |
| `frontend/src/bridgeDefinition/semanticParity/mac.test.ts` | MAC テスト |

---

## 33. acceptance criteria

1. **static result parity**: 2つの等価モデル（node/member ID 不同）から得られた AnalysisResult が、resultParity コアにより "equivalent" と判定されること。
2. **eigen result parity**: eigenvalue, frequency, period が tolerance 内で一致。mode shape が MAC > 0.95 で同値判定されること。
3. **I/J reversal robustness**: member の nodeI/nodeJ を入れ替えても、result parity が "equivalent" を返すこと。
4. **±sign equivalence**: mode shape の符号が反転しても parity が "equivalent" を返すこと。
5. **non-finite guard**: NaN/Infinity を含む result は parity が "invalid" を返すこと。
6. **empty/failed result guard**: `errors.length > 0` の result は parity が "invalid" を返すこと。
7. **既存テスト回帰**: `resultViewModel.test.ts`, `test_engine_verification_cases.py`, `test_eigen_analysis.py` が全て Pass。

---

## 34. PR 境界

**PR3 の範囲**:
- result parity core (`resultParity.ts`, `eigenParity.ts`)
- result normalization (`resultNormalize.ts`)
- MAC utility (`mac.ts`)
- テストファイル
- index.ts からの export

**PR3 に含めないもの**:
- backend 変更
- Viewer 変更
- API 変更
- Golden file の更新
- semanticParity/types.ts への ParityReport 拡張（既存 ParityReport に結果カテゴリを追加する場合のみ含む）

---

## 35. blocker

1. **I/J 反転時の正確な変換規則**: `element.py` の回転行列数学から導出が必要。fixture で backend 結果を実測して確認するまで **推定** が残る。
2. **near-repeated eigenvalue matching**: solver の数値安定性に依存。fixture での検証が必要。
3. **MAC threshold**: 0.95 は推奨値。プロジェクト固有の許容値との兼ね合い。
4. **result 永続化の不在**: `ProjectModel.analysisResults` に static/eigen がないため、parity test では API 経由か手書き fixture を使う必要がある。

---

## STEP8_6_SCOPE_VERDICT: PASS
## STEP8_6_SCOPE_REASON: 全35項目を調査。型定義・backend実装・frontend表示・parity core 拡張点が明確。I/J反転規則とMAC実装が主な実装リスクだが、既存tolerance/matching基盤で吸収可能。backlogな項目はない。
