まず、既存の仕様文書とソースコードを並行して読み込みます。
次にソースコードファイルを並行して読み込みます。
残りのソースコードファイルを並行して読み込みます。
最後に既存テストとtolerance設定を確認します。
十分な情報が揃いました。Step 8.5スコープレポートを生成して保存します。
レポートを保存しました。標準出力に出力します。
保存先: `docs/liner/phase4.5/implementation/step8_5_mimo_scope_20260711-213000.md` (949行)

レポートは30章で構成。主な発見:

- **NormalizedModel に load 配列を optional で追加**すれば既存 API に影響なし
- **Load case identity:** `(name, type)` を semantic key とし、`id` は trace metadata
- **Nodal/member load:** 既マッチ済み node/member endpoint key 経由で関連付け
- **Explicit zero vs absent:** load が存在するが全コンポーネント 0 → equivalent; 片側のみ → different
- **I/J 反転:** `coordinateSystem: "global"` は同一判定、`"local"` は warning diagnostic
- **変更候補ファイル:** `types.ts`, `normalize.ts`, `compare.ts`, `serializer.ts`, `generatedModelParity.ts`, `index.ts` の6ファイル拡張 + `loadParity.ts` 新規
- **Backend/generator/adapter は変更不要**
- **Unsupported domain 12項目**を明示的に非スコープ化
