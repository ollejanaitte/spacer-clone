# Images

This directory stores screenshots and visual assets referenced by the public README and documentation.

Expected files:

- `app-overview.png` - main application overview
- `analysis-results.png` - analysis result tables and viewer
- `liner-workflow.png` - LINER workflow
- `dxf-export.png` - DXF / CAD export preview

Current status:

| Gap | Status | Owner | Gate |
| --- | --- | --- | --- |
| Public screenshots | DOCUMENT_GAP | Documentation owner + UI/release owner | Reproducible stable UI states and approved non-confidential sample data before public release |

README placeholders already exist as HTML comments in the repository root `README.md`. Expected files
remain proposals until approved screenshots are added; this index does not claim that they exist.

Guidelines:

- Prefer PNG for screenshots.
- Keep images reasonably compressed.
- Do not include confidential project data.
- Use stable UI states that are reproducible from sample models under `examples/`.
