# 06 — Current Geometry Source Map (detail)

See also `current_sample_data_flow.md` and `component_source_trace_matrix.csv`.

Primary SoR: `apolloBridgeStructureInput` draft (schema `1.1.0-development`).  
Derived: BSDD structural model, C1 geometry kernel, solids, STL, quantity, loads, analysis adapter.  
Forbidden reverse path: mesh → design numbers (still prohibited).
