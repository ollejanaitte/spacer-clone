# RcDeckHaunchModel

Per main girder. shapeType RECT|TRAPEZOID. Datum: top flange upper face to deck soffit (DEC-S4-0009).

Projected to BSSD `StructuralDesignModel.haunches` with additive geometry fields when complete. EXPLICIT_NONE → `haunches: []` with presence retained in Apollo input.
