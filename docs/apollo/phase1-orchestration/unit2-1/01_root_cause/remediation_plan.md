# Apollo Phase 1-NN Unit 2.1 Remediation Plan

1. Align the repository's formal Linux startup path with Apollo verification by adding `./start` and `./start --apollo`.
2. Ensure Apollo verification scripts use real clicks instead of synthetic click dispatch.
3. Preserve Electron interaction safety by marking Apollo interactive controls as `no-drag`.
4. Re-run Electron verification through the corrected Apollo launch path before any Unit 3 scope expansion.
