# Apollo Phase 1-NN Unit 2.1 Interaction Sequence

1. Launch `./start` and confirm the launcher reports `APOLLO_MODE=1`.
2. Launch `./start --apollo` and confirm it reports the same Apollo mode and Electron command path.
3. Run Electron interaction verification with real Playwright clicks.
4. Confirm Apollo entry click-through, editor add/select flows, save/reload, numeric guard, and publication guard.
