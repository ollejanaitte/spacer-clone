# Phase 1-NN Stop Conditions

- a non-numeric change requires solver execution
- a non-numeric change requires authoritative numeric output
- a non-numeric change needs hardcoded engineering values
- a verified badge or authoritative publication path becomes necessary
- `origin/main` advances before push and requires merge or rebase
- a change would overwrite or discard unrelated user work
