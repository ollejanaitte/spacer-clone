# 16 — Sample Generation Pipeline

```
select sample → disclaimer → apply complete preset → (optional) generate
  → BSDD → solids (incl. pavement/marking viz) → qty → load → analysis hookup
  → viewer THREED_READY + labels
```

Reuse existing generate path; extend for pavement/marking builders. No mesh→qty reverse.
