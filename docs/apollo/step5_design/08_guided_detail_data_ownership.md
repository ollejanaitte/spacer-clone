# 08 — Guided / Detail Data Ownership

## Single SoR
`apolloBridgeStructureInput` (+ pavement/marking fields in same draft or versioned sibling under project Apollo namespace).

## Rules
- Guided Mode writes the same draft objects as detail panels
- No Guided-only bridge model / no sample-only fork
- Detail escape returns to same slide theme without copying data
- Save/reload uses existing project persistence + checksum STALE rules
