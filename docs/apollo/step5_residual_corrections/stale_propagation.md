# STALE propagation (Step 5-R)

## Clears `generatedAt` (STALE)

Via existing `with*` helpers:

- Sample replace / field edits
- `withLateralAngleSection` (L orientation/dims)
- `withCrossFrameAttachment` (pattern / depths)
- Girder depth / flange / spacing / sway interval field writes

## Downstream

Regenerate restores CURRENT for 3D / STL / quantity consumers that read BSDD + draft.

Step 4-G report/drawing/ZIP reintegration remains pending (out of scope).
